# Context Layer

This folder is reserved for production React context providers when shared state is required.

Current upload flow uses local component state and hooks to keep concerns isolated and simple.
